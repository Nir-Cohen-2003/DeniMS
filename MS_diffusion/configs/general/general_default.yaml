# General settings
name: null

wandb: 'disabled'      # online | offline | disabled
wandb_project: graph_ddm_ms
wandb_api_key: null
gpus: 1  # Multi-gpu is not implemented on this branch

resume: null
test_only: null

check_val_every_n_epochs: 5
sample_every_val: 8
val_check_interval: null
samples_to_generate: 256       # Number of samples to generate, or "all" to use all samples in val/test set
samples_to_save: 10
chains_to_save: 5
log_every_steps: 50
number_chain_steps: 50        # Number of frames in each gif
test_iterations: 50            # Number of test iterations for evaluation
compute_mces: true
mces_timeout_sec: 120   # set to null to disable timeout

final_model_samples_to_generate: 32
final_model_samples_to_save: 30
final_model_chains_to_save: 20

evaluate_all_checkpoints: False